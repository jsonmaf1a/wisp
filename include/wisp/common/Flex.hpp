#pragma once

enum class FlexDirection
{
    Row,
    Column
};

enum class FlexJustification
{
    Start,
    Center,
    End,
    SpaceBetween,
};

enum class FlexAlignment
{
    Start,
    Center,
    End,
};
